---
phase: 03-multi-agent-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/plan/trace.ts
  - src/lib/plan/schema.ts
  - src/lib/plan/cache.ts
  - data/runs/.gitkeep
  - .gitignore
autonomous: true
requirements: [AGENT-03]
tags: [phase-3, multi-agent, foundation, schema, trace-events, cache]

must_haves:
  truths:
    - "AgentEvent discriminated-union schema is importable by both server route and Phase 6 trace rail"
    - "Plan schema with 5 sections (protocol, materials, budget, timeline, validation) is importable from one source of truth"
    - "Plan cache supports in-memory Map AND disk read-through from data/runs/<run_id>.json (D-64)"
    - "data/runs/ directory exists and is gitignored except for .gitkeep marker"
  artifacts:
    - path: "src/lib/plan/trace.ts"
      provides: "AgentEvent discriminated-union schema (D-62) — Phase 6 unblocker"
      exports: ["agentEventSchema", "agentIdSchema", "AgentEvent", "AgentId"]
    - path: "src/lib/plan/schema.ts"
      provides: "Top-level Plan Zod schema with 5 sections + agent_artifacts (D-58)"
      exports: ["planSchema", "Plan", "protocolStepSchema", "materialRowSchema", "budgetLineSchema", "timelinePhaseSchema", "validationCheckSchema", "complianceNoteSchema", "agentArtifactSchema"]
    - path: "src/lib/plan/cache.ts"
      provides: "In-memory runStore + cacheIndex + disk read-through (D-64, D-65)"
      exports: ["hashHypothesis", "getCachedRun", "setCachedRun", "getRunById", "runStore"]
    - path: "data/runs/.gitkeep"
      provides: "Directory marker so fs.writeFile target exists at cold start"
  key_links:
    - from: "src/lib/plan/schema.ts"
      to: "src/lib/qc/schema.ts"
      via: "import { citationSchema }"
      pattern: "import.*citationSchema.*from.*qc/schema"
    - from: "src/lib/plan/cache.ts"
      to: "src/lib/plan/schema.ts"
      via: "import type { Plan }"
      pattern: "import.*Plan.*from.*\\./schema"
---

<objective>
Ship Phase 3 foundations: the three pure-data modules (`trace.ts`, `schema.ts`, `cache.ts`) and the runtime artifact directory (`data/runs/`). These have ZERO LLM dependencies — they are pure Zod + Node fs work.

**Why Wave 1 in isolation:** `trace.ts` (the `AgentEvent` schema) MUST land first to unblock the parallel Phase 6 chat. Phase 6 cannot start its trace-rail wire-in until this type exists. Schema + cache co-ship because the Wave 2 agent runners import both.

**What ships:** 3 new TypeScript modules + 1 directory marker + 1 .gitignore append. No LLM calls, no streaming. Pure module-level code that compiles green.

**What does NOT ship:** Any agent prompt or LLM call (Plan 03-02), the route handler or client hook (Plan 03-03), the dashboard wire-in (Plan 03-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-agent-pipeline/03-CONTEXT.md
@.planning/phases/03-multi-agent-pipeline/03-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing exports the Phase 3 foundations import or mirror. Extracted via grep -->
<!-- so the executor needs no codebase exploration. -->

From src/lib/qc/schema.ts (citationSchema reused per D-58):
```typescript
export const citationSchema = z.object({
  title: z.string().min(1).describe("Paper or protocol title from the Tavily result."),
  url: z.string().url().describe("Source URL — MUST be one of the URLs in the supplied evidence block."),
  excerpt: z.string().min(1).max(280),
  source: z.enum(["arxiv", "semantic-scholar", "protocols-io", "other"]),
});
export const qcResponseSchema = z.discriminatedUnion("ok", [...]);
export type QCResponse = z.infer<typeof qcResponseSchema>;
```

From src/lib/qc/cache.ts (Phase 2 pattern to extend with disk read-through):
```typescript
export async function hashHypothesis(input: string): Promise<string>; // SHA-256 of trim().toLowerCase()
export function getCached(key: string): QCResponse | undefined;
export function setCached(key: string, value: QCResponse): void;
```

From src/components/trace-rail.tsx (the 6 validation names Skeptic must emit verbatim — informs schema's `validation.min(5)` floor):
```typescript
const VALIDATION_SKELETON = [
  "Every reagent has a catalog URL",
  "Budget sums correctly",
  "No orphan protocol step",
  "Citations resolve to real sources",
  "Timeline dependencies valid",
  "Compliance pipeline passes",
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AgentEvent schema at src/lib/plan/trace.ts (D-62, Phase 6 unblocker)</name>
  <files>src/lib/plan/trace.ts</files>
  <read_first>
    - .planning/phases/03-multi-agent-pipeline/03-CONTEXT.md (D-61, D-62 — agent lifecycle + schema shape)
    - .planning/phases/03-multi-agent-pipeline/03-PATTERNS.md (section "src/lib/plan/trace.ts (NEW)")
    - src/lib/qc/schema.ts (analog — discriminated-union pattern at lines 35-59)
  </read_first>
  <action>
    Create `src/lib/plan/trace.ts` exporting `agentIdSchema`, `agentEventSchema`, and types `AgentEvent` + `AgentId`. Mirror the discriminated-union pattern from `src/lib/qc/schema.ts:35-59` but discriminate on `stage` not `ok`.

    File contents (write verbatim):
    ```typescript
    /**
     * AgentEvent discriminated-union schema (D-61, D-62).
     *
     * Single source of truth for trace events that flow:
     *   server route (writer.write({type:"data-trace", data: AgentEvent}))
     *   → SSE wire
     *   → useChat onData callback in src/components/plan/use-plan.ts
     *   → Phase 6 trace-rail render switch on `event.stage`.
     *
     * Discriminator field is `stage` (not `ok` like qcResponseSchema) so the same
     * Zod narrow pattern from Phase 2 D-40 transfers verbatim:
     *   if (!event?.stage) return null;
     *   switch (event.stage) { case "started": ...; case "done": ...; }
     *
     * Per-agent lifecycle: started → working* → (done | error). Total per run
     * ≈ 12-15 events (4 agents × 3 + consolidator events). Keep working events
     * to ~1-2 per agent — no token-level streaming (avoids re-render storms).
     */
    import { z } from "zod";

    export const agentIdSchema = z.enum([
      "researcher",
      "skeptic",
      "operator",
      "compliance",
      "consolidator",
    ]);
    export type AgentId = z.infer<typeof agentIdSchema>;

    export const agentEventSchema = z.discriminatedUnion("stage", [
      z.object({
        stage: z.literal("started"),
        run_id: z.string(),
        agent_id: agentIdSchema,
        task: z.string().min(1).max(280),
        ts: z.string(),
      }),
      z.object({
        stage: z.literal("working"),
        run_id: z.string(),
        agent_id: agentIdSchema,
        activity: z.string().min(1).max(280),
        ts: z.string(),
      }),
      z.object({
        stage: z.literal("done"),
        run_id: z.string(),
        agent_id: agentIdSchema,
        elapsed_ms: z.number().nonnegative(),
        token_count: z.number().int().nonnegative().optional(),
        output_preview: z.string().max(120).optional(),
        ts: z.string(),
      }),
      z.object({
        stage: z.literal("error"),
        run_id: z.string(),
        agent_id: agentIdSchema,
        error_message: z.string().min(1).max(400),
        retryable: z.boolean(),
        ts: z.string(),
      }),
    ]);
    export type AgentEvent = z.infer<typeof agentEventSchema>;
    ```

    Do NOT add re-exports of other modules. Do NOT import anything from `src/lib/plan/schema.ts` or `cache.ts` (this file must be import-leaf so Phase 6 can pull it without dragging in unrelated deps).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "(error|trace\.ts)" || echo "TYPECHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/lib/plan/trace.ts` returns 0
    - Exports `agentEventSchema`: `grep -c "export const agentEventSchema" src/lib/plan/trace.ts` returns 1
    - Exports `agentIdSchema`: `grep -c "export const agentIdSchema" src/lib/plan/trace.ts` returns 1
    - Exports both types: `grep -cE "export type (AgentEvent|AgentId)" src/lib/plan/trace.ts` returns 2
    - Uses `z.discriminatedUnion("stage"`: `grep -c 'z.discriminatedUnion("stage"' src/lib/plan/trace.ts` returns 1
    - All 4 stages present: `grep -cE 'z.literal\("(started|working|done|error)"\)' src/lib/plan/trace.ts` returns 4
    - 5 agent IDs present: `grep -cE '"(researcher|skeptic|operator|compliance|consolidator)"' src/lib/plan/trace.ts` returns at least 5
    - No imports from `./schema` or `./cache` (leaf module): `grep -cE 'from "\\./(schema|cache)"' src/lib/plan/trace.ts` returns 0
    - tsc passes: `npx tsc --noEmit` shows no errors mentioning `trace.ts`
  </acceptance_criteria>
  <done>File compiles, exports the schema + 2 types, discriminated-union shape matches D-62 verbatim, no cross-module imports.</done>
</task>

<task type="auto">
  <name>Task 2: Create Plan schema at src/lib/plan/schema.ts (D-58, 5-section flat shape)</name>
  <files>src/lib/plan/schema.ts</files>
  <read_first>
    - .planning/phases/03-multi-agent-pipeline/03-CONTEXT.md (D-57 ownership map, D-58 schema shape, D-59 grounded:false)
    - .planning/phases/03-multi-agent-pipeline/03-PATTERNS.md (section "src/lib/plan/schema.ts (NEW)" — full row schemas)
    - src/lib/qc/schema.ts (citation reuse + discriminated-union pattern)
  </read_first>
  <action>
    Create `src/lib/plan/schema.ts` exporting all per-row schemas + the top-level `planSchema` and the `Plan` type. Reuse `citationSchema` from `@/lib/qc/schema` — do NOT redefine it (D-58).

    File contents (write verbatim — every field name and constraint comes from D-58 in CONTEXT.md):
    ```typescript
    /**
     * Plan response Zod schema (D-58).
     *
     * Single source of truth for both the server route AND the client hook.
     * Imports `citationSchema` from QC's schema (D-58 reuse — do NOT redefine).
     *
     * Phase 3 emits citations: [] for every row and grounded: false at the top.
     * Phase 5 fills citations and flips the flag. Phase 4 reads this schema to
     * render the 5 tabs (Protocol/Materials/Budget/Timeline/Validation).
     */
    import { z } from "zod";
    import { citationSchema } from "@/lib/qc/schema";

    export const protocolStepSchema = z.object({
      step_number: z.number().int().positive(),
      description: z.string().min(1).max(800),
      duration_estimate: z.string().min(1).max(120),
      citations: z.array(citationSchema).default([]),
    });

    export const materialRowSchema = z.object({
      name: z.string().min(1).max(200),
      quantity: z.string().min(1).max(80),
      supplier: z.string().nullable(),
      catalog_number: z.string().nullable(),
      unit_price_usd: z.number().nullable(),
      citations: z.array(citationSchema).default([]),
    });

    export const budgetLineSchema = z.object({
      category: z.string().min(1).max(120),
      amount_usd: z.number().nonnegative(),
      notes: z.string().max(280).optional(),
      citations: z.array(citationSchema).default([]),
    });

    export const timelinePhaseSchema = z.object({
      phase: z.string().min(1).max(120),
      duration_days: z.number().int().positive(),
      depends_on: z.array(z.string()).default([]),
      citations: z.array(citationSchema).default([]),
    });

    export const validationCheckSchema = z.object({
      name: z.string().min(1).max(120),
      description: z.string().min(1).max(400),
      measurement_method: z.string().min(1).max(280),
      pass_criteria: z.string().min(1).max(280),
    });

    export const complianceNoteSchema = z.object({
      target_kind: z.enum(["protocol_step", "material_row", "global"]),
      target_index: z.number().int().nonnegative().nullable(),
      note: z.string().min(1).max(600),
      severity: z.enum(["info", "caution", "blocking"]),
    });

    export const agentArtifactSchema = z.object({
      raw_draft: z.unknown(),
      model_id: z.string(),
      elapsed_ms: z.number().nonnegative(),
      token_count: z.number().int().nonnegative().optional(),
      error: z.string().optional(),
    });

    export const planSchema = z.object({
      run_id: z.string(),
      hypothesis: z.string(),
      qc_run_id: z.string().nullable(),
      grounded: z.boolean(),
      plan: z.object({
        protocol: z.array(protocolStepSchema),
        materials: z.array(materialRowSchema),
        budget: z.array(budgetLineSchema),
        timeline: z.array(timelinePhaseSchema),
        validation: z.array(validationCheckSchema).min(5),
      }),
      agent_artifacts: z.object({
        researcher: agentArtifactSchema,
        skeptic: agentArtifactSchema,
        operator: agentArtifactSchema,
        compliance: agentArtifactSchema,
      }),
      compliance_notes: z.array(complianceNoteSchema).default([]),
      compliance_summary: z.string(),
      model_id: z.string(),
      latency_ms: z.number().nonnegative(),
      generated_at: z.string(),
    });

    export type Plan = z.infer<typeof planSchema>;
    export type ProtocolStep = z.infer<typeof protocolStepSchema>;
    export type MaterialRow = z.infer<typeof materialRowSchema>;
    export type BudgetLine = z.infer<typeof budgetLineSchema>;
    export type TimelinePhase = z.infer<typeof timelinePhaseSchema>;
    export type ValidationCheck = z.infer<typeof validationCheckSchema>;
    export type ComplianceNote = z.infer<typeof complianceNoteSchema>;
    export type AgentArtifact = z.infer<typeof agentArtifactSchema>;
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "schema\.ts.*error" || echo "TYPECHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `src/lib/plan/schema.ts`
    - Imports citationSchema from qc: `grep -c 'import { citationSchema } from "@/lib/qc/schema"' src/lib/plan/schema.ts` returns 1
    - 7 row schemas exported: `grep -cE "export const (protocolStep|materialRow|budgetLine|timelinePhase|validationCheck|complianceNote|agentArtifact)Schema" src/lib/plan/schema.ts` returns 7
    - Top-level planSchema exported: `grep -c "export const planSchema" src/lib/plan/schema.ts` returns 1
    - Validation floor of 5: `grep -c 'z.array(validationCheckSchema).min(5)' src/lib/plan/schema.ts` returns 1
    - Plan type exported: `grep -c "export type Plan = z.infer" src/lib/plan/schema.ts` returns 1
    - All 5 sections present in `plan` nested object: `grep -cE "  (protocol|materials|budget|timeline|validation):" src/lib/plan/schema.ts` returns 5
    - `grounded: z.boolean()` present: `grep -c "grounded: z.boolean()" src/lib/plan/schema.ts` returns 1
    - tsc passes: `npx tsc --noEmit` reports no errors in `schema.ts`
  </acceptance_criteria>
  <done>File compiles, all 5 plan sections + agent_artifacts + per-row citation slots present, Plan type inferable, validation.min(5) enforces TRACE-03 floor.</done>
</task>

<task type="auto">
  <name>Task 3: Create plan cache at src/lib/plan/cache.ts + data/runs/.gitkeep + .gitignore append (D-64, D-65)</name>
  <files>src/lib/plan/cache.ts, data/runs/.gitkeep, .gitignore</files>
  <read_first>
    - .planning/phases/03-multi-agent-pipeline/03-CONTEXT.md (D-64, D-65 — in-memory + disk read-through)
    - .planning/phases/03-multi-agent-pipeline/03-PATTERNS.md (section "src/lib/plan/cache.ts (NEW)" — full implementation)
    - src/lib/qc/cache.ts (analog — Map + hashHypothesis pattern; Phase 3 EXTENDS with disk fallback)
    - .gitignore (current contents — Edit, do NOT replace per CLAUDE.md global §4.11)
  </read_first>
  <action>
    Three sub-steps. Do them in order:

    **Step A — Write `src/lib/plan/cache.ts`** (clone qc/cache.ts pattern, extend with disk read-through):
    ```typescript
    /**
     * In-memory Plan cache + run store + disk read-through (D-64, D-65).
     *
     * Three structures:
     *   - cacheIndex: Map<hypothesis_hash, run_id>  (D-65 — dedupe by hypothesis)
     *   - runStore:   Map<run_id, Plan>             (D-64 — in-memory live cache)
     *   - data/runs/<run_id>.json                   (D-64 — disk read-through; gitignored)
     *
     * Cold-start re-runs: in-memory clears, but disk JSONs survive — getCachedRun
     * checks memory first, then disk. Phase 7's diff modal reads two JSON files
     * for side-by-side compare.
     *
     * NO DB (CLAUDE.md hard rule #6). Module is server-only — depends on
     * Node `crypto.subtle` and `fs/promises`. Importing from a client component
     * will throw at build time.
     */
    import { promises as fs } from "node:fs";
    import path from "node:path";
    import type { Plan } from "./schema";

    const cacheIndex = new Map<string, string>(); // hypothesis_hash → run_id
    export const runStore = new Map<string, Plan>(); // run_id → Plan

    const RUNS_DIR = path.join(process.cwd(), "data", "runs");

    export async function hashHypothesis(input: string): Promise<string> {
      const normalized = input.trim().toLowerCase();
      const buf = await crypto.subtle.digest(
        "SHA-256",
        new TextEncoder().encode(normalized),
      );
      return Array.from(new Uint8Array(buf))
        .map((b) => b.toString(16).padStart(2, "0"))
        .join("");
    }

    /**
     * D-64: in-memory write + fire-and-forget disk write. Errors logged, never thrown.
     */
    export function setCachedRun(hypothesis_hash: string, plan: Plan): void {
      cacheIndex.set(hypothesis_hash, plan.run_id);
      runStore.set(plan.run_id, plan);
      void fs
        .mkdir(RUNS_DIR, { recursive: true })
        .then(() =>
          fs.writeFile(
            path.join(RUNS_DIR, `${plan.run_id}.json`),
            JSON.stringify(plan, null, 2),
          ),
        )
        .catch((err) =>
          console.error(
            JSON.stringify({
              event: "plan.disk.write_failed",
              run_id: plan.run_id,
              error: String(err),
            }),
          ),
        );
    }

    /**
     * D-64: cold-start fall-through. Memory hit → return; miss → check disk;
     * disk hit → re-hydrate memory + return; both miss → undefined.
     */
    export async function getCachedRun(
      hypothesis_hash: string,
    ): Promise<Plan | undefined> {
      const run_id = cacheIndex.get(hypothesis_hash);
      if (!run_id) return undefined;
      const inMem = runStore.get(run_id);
      if (inMem) return inMem;
      try {
        const text = await fs.readFile(
          path.join(RUNS_DIR, `${run_id}.json`),
          "utf8",
        );
        const plan = JSON.parse(text) as Plan;
        runStore.set(run_id, plan);
        return plan;
      } catch {
        return undefined;
      }
    }

    export function getRunById(run_id: string): Plan | undefined {
      return runStore.get(run_id);
    }
    ```

    **Step B — Create `data/runs/.gitkeep`** (empty file — directory marker):
    Use the Write tool to create an empty file at `data/runs/.gitkeep`. Content: empty string OR a single comment line (`# Phase 3 run-artifact directory marker (D-64). Gitignored except this file.`). Either works.

    **Step C — Append to `.gitignore`** (use Edit tool, do NOT rewrite the file per CLAUDE.md global §4.11):
    Append these lines to the end of `.gitignore`:
    ```
    
    # Phase 3 — multi-agent run artifacts (D-64). Disk read-through cache; never commit.
    data/runs/*.json
    !data/runs/.gitkeep
    ```

    The negation pattern preserves the directory marker so the directory survives `git clone`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "cache\.ts.*error" || echo "TYPECHECK_OK"; test -f src/lib/plan/cache.ts &amp;&amp; test -f data/runs/.gitkeep &amp;&amp; grep -q "data/runs/\*.json" .gitignore &amp;&amp; grep -q "!data/runs/.gitkeep" .gitignore &amp;&amp; echo "FILES_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/plan/cache.ts` exists
    - `data/runs/.gitkeep` exists: `test -f data/runs/.gitkeep` returns 0
    - `.gitignore` contains the include rule: `grep -c "data/runs/\\*.json" .gitignore` returns 1
    - `.gitignore` contains the negation: `grep -c "!data/runs/.gitkeep" .gitignore` returns 1
    - Cache exports: `grep -cE "^export (function|const|async function) (hashHypothesis|setCachedRun|getCachedRun|getRunById|runStore)" src/lib/plan/cache.ts` returns 5
    - Cache uses crypto.subtle: `grep -c "crypto.subtle.digest" src/lib/plan/cache.ts` returns 1
    - Cache reads from data/runs: `grep -c '"data", "runs"' src/lib/plan/cache.ts` returns 1
    - Cache imports Plan type: `grep -c 'import type { Plan } from "./schema"' src/lib/plan/cache.ts` returns 1
    - Disk fallback present: `grep -c "fs.readFile" src/lib/plan/cache.ts` returns 1
    - No raw `process.env`: `grep -c "process\\.env" src/lib/plan/cache.ts` returns 0 (we use `process.cwd()` only — that's fine; this check confirms no env leak)
    - tsc passes: `npx tsc --noEmit` reports no errors in `cache.ts`
    - .gitignore was MERGED (not replaced): `wc -l .gitignore` returns ≥18 (was 14 lines pre-Phase 3; now ≥18 after appending 4 lines)
  </acceptance_criteria>
  <done>cache.ts compiles, exports 5 symbols, disk read-through wired. data/runs/.gitkeep exists. .gitignore correctly excludes JSONs but tracks the marker.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run end-to-end foundation check:

```bash
# All 4 new files exist
test -f src/lib/plan/trace.ts && test -f src/lib/plan/schema.ts && test -f src/lib/plan/cache.ts && test -f data/runs/.gitkeep

# TypeScript compiles green across all 3 modules
npx tsc --noEmit

# Schema imports work end-to-end (compile-only smoke test):
node -e "require('typescript')" 2>/dev/null && npx tsc --noEmit src/lib/plan/schema.ts src/lib/plan/trace.ts src/lib/plan/cache.ts 2>&1 | head -20

# .gitignore correctly merged (the negation pattern is the load-bearing line)
git check-ignore data/runs/foo.json && ! git check-ignore data/runs/.gitkeep && echo "GITIGNORE_OK"
```

The third command is the proof Phase 3 disk-write artifacts will be ignored while the directory marker survives `git clone`.
</verification>

<success_criteria>
1. `src/lib/plan/trace.ts` exports the 4-stage `agentEventSchema` discriminated union; Phase 6 chat can begin importing this type.
2. `src/lib/plan/schema.ts` exports `planSchema` with 5 sections (protocol/materials/budget/timeline/validation) plus `agent_artifacts` + `compliance_summary` + `compliance_notes`. `validation.min(5)` enforces the TRACE-03 floor at the schema layer.
3. `src/lib/plan/cache.ts` exports in-memory `runStore` + disk read-through via `getCachedRun`. Phase 7's diff modal will reuse this read path.
4. `data/runs/` exists with `.gitkeep`; `data/runs/*.json` is gitignored.
5. Full project `npx tsc --noEmit` is green (no new TS errors introduced).
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-agent-pipeline/03-01-SUMMARY.md` capturing:
- Files created (with line counts)
- Any deviation from the prescribed schemas (none expected — D-58 / D-62 are locked)
- The single line in `.gitignore` change (the 3 appended lines + 1 blank separator)
- Confirmation that `git check-ignore data/runs/foo.json` returns 0 and `git check-ignore data/runs/.gitkeep` returns non-zero
- Note for parallel Phase 6 chat: trace.ts is committed and the `AgentEvent` type is now importable from `@/lib/plan/trace`
</output>
