---
context: between_phases
phase: 07-closed-loop-corrections
task: null
total_tasks: null
status: ready_to_plan
last_updated: 2026-04-26T11:09:47.362Z
---

# Sextant — Resume from Phase 7 boundary

## Headline

Phase 4 + Phase 5 LITE just shipped end-to-end. Production verified working. Phase 7 (the moat moment) is next. Submission deadline ~2026-04-26 13:00 ET (~3h headroom).

<current_state>
Phase 4 (Plan Canvas UI) + Phase 5 LITE (Materials grounding) are complete and pushed to origin/main. Vercel auto-deployed. User confirmed end-to-end test of chip h1 (CRP biosensor) works: verdict + 5-tab plan canvas + Tavily-grounded materials with clickable [N sources] citations all render correctly.

Remaining work:
- Phase 7: Closed-loop corrections + propagation demo (the moat moment)
- Phase 8: Cache-warm + record demo + submission form

Token budget tight: user is at ~70% of 5h Claude Max limit. Strategy: skip plan-checker for Phase 7 (--skip-verify flag), keep inline execution where possible.
</current_state>

<completed_this_session>

This session shipped:
- **Phase 4: Plan Canvas UI** — 3 waves, 10 commits, ~10m wall. 5-tab canvas (Protocol/Materials/Budget/Timeline/Validation) with shadcn Tabs primitive, keyboard nav, 3-state coexistence (hero/skeleton/tabs).
- **Phase 5 LITE: Materials Grounding** — 4 commits, ~4m wall. 1 Tavily call per Plan, broadcasts citations to all material rows, CitationSlot now a clickable link with native tooltip.
- **UI polish fixes (5 commits)** — orphan-step validation semantic relaxed; trace-rail label overlap fixed (sextant-loader hides labels in `sm` size); 3 Open-Sextant landing buttons uniform black (hero kept gradient shimmer); footer specificity bump to fix invisible text-on-black.
- **Documentation** — `.planning/DEFERRED.md` (22 explicit scope cuts with cost estimates), Phase 4 SUMMARY.md ×3, Phase 5 SUMMARY.md.

Total commits this session: ~17 to origin/main.
</completed_this_session>

<remaining_work>

### Phase 7 — Closed-Loop Corrections (the moat moment)

**Hard scope cut per CLAUDE.md hard rule #3:**
- "Correct" action ONLY — no Challenge, no Annotate
- No lab profile drawer (LOOP-05 deferred)
- No per-line rule labels in diff modal (PROP-04 deferred)
- Cross-hypothesis rule generalization deferred

**Must-have for demo:**
- Click any plan line → "Correct" popover with text input
- Submit → POST /api/lab-rules → typed rule extracted via 1 Gemini call (rule, scope, reasoning, source_correction)
- Persist to data/lab_rules.json (no DB — file-based per CLAUDE.md hard rule #6)
- Header "0 lab rules" pill updates from 0 → 1
- On NEXT plan generation: GET lab rules + inject into all 4 agent prompts
- After Plan B renders, "Compare with previous plan" button → modal with side-by-side diff (Plan A vs Plan B with changed lines highlighted in clay/rust accent)
- Pre-stage 2 demo hypotheses where the same rule applies to both (e.g. "always use BSL-2 containment for whole blood" applies to both CRP biosensor + L. rhamnosus GG)

### Phase 8 — Polish + Demo + Submit

- Cache-warm 4 chips (5min before recording)
- Activate trace-rail demo-pace (`SEXTANT_DEMO_PACE_MS=3500` env or `?demoPace=slow` URL param)
- Record 60s demo screencast
- Record tech video (Mermaid architecture explainer)
- Fill SUBMISSION.md form fields (already drafted in parallel chat)
- Get team photo (user TODO)
- Submit
</remaining_work>

<decisions_made>

This session:
- Phase 5 inline-executed instead of full GSD planner+checker pipeline (saved ~250k tokens)
- Phase 7 should use `/gsd-plan-phase 7 --auto --skip-verify` (skip checker, save ~100k)
- Orphan-step validation check loosened to "every step has positive step_number" (citation cross-ref was too strict)
- Landing buttons: hero keeps gradient shimmer; header + footer matched to ink-black for uniformity
</decisions_made>

<blockers>

None. Production is fully working. Tavily env was rotated by user earlier this session, Gemini upgraded to paid tier. The HANDOFF.json blocker section is empty.
</blockers>

## Required Reading (in order)

1. `.planning/PROJECT.md` — vision, defensibility thesis, judge-facing pitch framing
2. `.planning/REQUIREMENTS.md` — LOOP-01..05 + PROP-01..04 are Phase 7 territory
3. `.planning/DEFERRED.md` — what we're cutting and why (Phase 7 has 6 explicit deferrals)
4. `CLAUDE.md` — hard rule #3 (cut Phase 7 to manual slide if not wired by hour 18) is the safety valve
5. `.planning/phases/05-grounding-citations/05-01-SUMMARY.md` — pattern for inline-execution if we go that route again
6. `src/lib/plan/schema.ts` — Plan type (Phase 7 reads, doesn't modify)
7. `src/app/api/plan/route.ts` — agent fan-out pattern; Phase 7 needs to inject lab rules into the prompts at lines ~140-180

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Half-built Phase 7 | The user has lab rules captured but no propagation visible in Plan B → demo falls flat | blocking | CLAUDE.md hard rule #3: if not wired by hour 18, CUT to manual before/after slide |
| Fake citations | Empty `citations: []` arrays must render NOTHING — never fabricate | blocking | CitationSlot returns null on empty (already enforced in src/components/plan/citation-slot.tsx line 34) |
| Modify locked schemas | src/lib/plan/schema.ts and src/lib/qc/schema.ts are LOCKED — Phase 7 must read, not modify | blocking | If Phase 7 needs new fields, add them to a sibling type (e.g. LabRule), don't extend Plan |
| New deps without justification | CLAUDE.md hard rule #5 forbids adding deps beyond locked stack | advisory | Phase 7 should be deliverable with current stack (Next.js + AI SDK + shadcn + lucide) |
| Burn tokens on plan-checker for low-stakes phases | Phase 5 LITE proved single-plan phases don't need the checker | advisory | Use --skip-verify for Phase 7 (CLAUDE.md hard rule #3 is the actual safety valve) |

## Infrastructure State

- **Production URL:** https://sextant-uekv.vercel.app — deployed + working as of last test
- **Vercel:** Auto-deploys on push to main; latest deploy includes Phase 4 + Phase 5 LITE + UI fixes
- **Tavily API:** Working (env rotated by user earlier this session)
- **Gemini API:** Paid tier (no rate limits expected for demo)
- **GitHub:** git@github.com:yauhenifutryn/sextant.git (PUBLIC)
- **Storage:** JSON files in repo. data/runs/ for plan cache. data/lab_rules.json will be created in Phase 7.
- **Local env:** .env.local has working ANTHROPIC_API_KEY, GOOGLE_GENERATIVE_AI_API_KEY, TAVILY_API_KEY

<context>
Heavy productive session. Phase 4 + Phase 5 LITE shipped end-to-end via mixed strategy: full GSD pipeline (planner + checker + 3 wave executors) for Phase 4, and inline execution (no subagents) for Phase 5 LITE. Token budget is now ~30% of 5h limit remaining.

The product works for any input, not just the 4 chips — the chips are just preset hypotheses for fast demo recording (cache-warmed). User can test arbitrary hypotheses live, but quality varies (Tavily may miss good supplier pages for less-common reagents).

Phase 7 IS the moat moment per PROJECT.md "Defensibility & Pitch Framing" section. The closed loop demo proves the thesis can close. Without it, the pitch reduces to "another GPT wrapper" — the demo's whole job is to show that user corrections compound into the next plan automatically. The diff modal is the visible proof.

Strategic note: the user has a NEW chat planned for Phase 7 (clean context). The recommended sequence is `/gsd-resume-work` → `/gsd-plan-phase 7 --auto --skip-verify`. The `--auto` chains to execute. `--skip-verify` saves ~100k tokens by dropping the plan-checker (CLAUDE.md hard rule #3 is the actual safety valve).
</context>

<next_action>
In the new chat after `/clear`:

1. Run `/gsd-resume-work` — orchestrator-only, ~10k tokens, restores context.

2. Run `/gsd-plan-phase 7 --auto --skip-verify` — spawns planner agent (~100k tokens), auto-chains to execute (~150k tokens). Total ~250k.

OR if budget is critical: tell Claude "draft Phase 7 PLAN.md inline like Phase 5 LITE, then execute it directly". Drops cost to ~150k but more error-prone for a phase this complex (5+ files, new API route, new client UI for popover, persistent JSON store, agent prompt modifications, diff modal).

Decision criterion: if 5h Max usage is < 75%, take the planner-agent path (safer). If > 75%, take the inline path (cheaper, with fallback to manual slide per hard rule #3).

Before recording the demo (Phase 8): cache-warm all 4 chips by submitting them once each via the dashboard, OR set SEXTANT_DEMO_PACE_MS=3500 env var so cache-hits don't flash all agents done in <100ms during recording.
</next_action>
